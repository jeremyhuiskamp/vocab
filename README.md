# Vocab

Vocab training app.  Generate sample sentences containing a word, then practise them by filling in blanks.

## Run

```bash
uv sync
uv run uvicorn app.main:app --reload
open http://127.0.0.1:8000
```
